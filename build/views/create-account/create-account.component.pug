.form-new-account
	h4 Estas dando un paso adelante
	form.input-form.form-group.mt-2()
		.form-group
			input.form-control(autocomplete="off",placeholder="Nombre",name="name",required="required",minlength="3",maxlength="64")
		.form-group
			input.form-control(autocomplete="off",placeholder="Apellidos",name="surname",required="required",minlength="3",maxlength="64")
		.form-group
			input.form-control(autocomplete="off",placeholder="Nombre de usuario",required="required",minlength="3",maxlength="64")
		.form-group
			input.form-control(autocomplete="off" type='text', placeholder='Correo electronico',name="email",required="required",maxlength="64",pattern="[a-z0-9]+(\.[_a-z0-9]+)*@[a-z0-9-]+(\.[a-z0-9-]+)*(\.[a-z]{2,15})")
		.form-group
			input.form-control(autocomplete="off" type='password', placeholder='Contraseña',required="required",minlength="6",maxlength="10")
		.form-group
			input.form-control(autocomplete="off", type='password', placeholder='Repetir Contraseña',required="required",minlength="6",maxlength="10")
		button.btn.btn-warning Registrarse
	.list-group
		a(routerLink="/login") ¿Tienes una cuenta? #[span Ingresa ahora]
